apiVersion: v1
kind: Service
metadata:
  name: useraccountmanagement-service
spec:
  selector:
    app: useraccountmanagement
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: NodePort
